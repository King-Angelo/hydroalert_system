# Superseded: the canonical workflow lives at repository root:
#   .github/workflows/build-admin-web-staging.yml
#
# This file is retained as a short reference only. Do not copy — use the root workflow.

name: (moved) Build admin web (staging)
# see ../../.github/workflows/build-admin-web-staging.yml
