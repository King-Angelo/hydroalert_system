name: hydroalert_backend_api
description: HydroAlert backend API scaffold using Dart Frog.
publish_to: none

environment:
  sdk: '^3.11.0'

dependencies:
  dart_frog: ^1.2.6

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
